<?xml version="1.0"?>
<ruleset name="CMW.WordPress">

	<!-- Add source codes in the report -->
	<arg value="s" />
	<arg name="colors" />

	<!-- Check all PHP files in directory tree by default. -->
	<arg name="extensions" value="php" />
	<file>.</file>

	<rule ref="WordPress">

		<!-- Allow slash-delimited Hooks -->
		<exclude name="WordPress.NamingConventions.ValidHookName.UseUnderscores" />

		<!-- Allow false for deprecated param -->
		<exclude name="WordPress.WP.DeprecatedParameters.Load_plugin_textdomainParam2Found" />

		<!-- I prefer my control structures -->
		<exclude name="WordPress.WhiteSpace.ControlStructureSpacing.BlankLineAfterEnd" />

		<!-- Allow unconventional filenames for now -->
		<exclude name="WordPress.Files.FileName.InvalidClassFileName" />

		<!-- Allow sample code in comment blocks -->
		<exclude name="Squiz.PHP.CommentedOutCode.Found" />

		<!-- Strict arrays need auditing manually -->
		<exclude name="WordPress.PHP.StrictInArray.MissingTrueStrict" />

		<!-- Retained for review -->
		<exclude name="WordPress.Security.EscapeOutput.OutputNotEscaped" />

	</rule>

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array" value="bp-groups-civicrm-sync" />
		</properties>
	</rule>

	<!-- Allow short array syntax -->
	<rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
		<severity>0</severity>
	</rule>
	<rule ref="Generic.Arrays.DisallowLongArraySyntax.Found" />

	<!-- Nesting levels -->
	<rule ref="Generic.Metrics.NestingLevel">
		<properties>
			<property name="absoluteNestingLevel" value="5" />
		</properties>
	</rule>

	<exclude-pattern>assets/civicrm/*</exclude-pattern>

</ruleset>
